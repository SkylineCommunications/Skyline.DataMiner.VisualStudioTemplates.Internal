# $InstallerName$.CA

## About

This is the project containing C# code which is executed as custom actions during execution of the MSI file.
These custom actions can be used to perform any tasks that are required during installation, upgrade, deletion, rollback,...